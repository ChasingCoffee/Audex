---
phase: 02-bass-audio-integration
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/Audex/UI/ControlBarRenderer.cs
  - src/Audex/UI/LayoutRenderer.cs
  - src/Audex/UI/ThemeHelper.cs
  - src/Audex/UI/PreviewWindow.cs
  - src/Audex/PreviewHandler/AudioPreviewHandler.cs
autonomous: false
requirements: [PLAY-01, PLAY-03, FMT-01, META-01, META-02]

must_haves:
  truths:
    - "User can click play button and hear audio through WASAPI output"
    - "User can pause playback and resume from the same position"
    - "User can stop playback (resets position to start, button shows Play)"
    - "User can adjust volume via horizontal slider, independently of system volume"
    - "User can mute/unmute by clicking speaker icon"
    - "Seek bar shows elapsed time on left and total time on right (1:23 ━━━━ 4:56)"
    - "User can click seek bar to jump to position in track"
    - "Preview displays sample rate, bit depth, channels, format in grid layout"
    - "Preview displays title, artist, album from tags (hidden if missing)"
    - "WAV, MP3, and FLAC files play correctly"
    - "Switching files while playing stops current and loads new file"
    - "Volume persists across sessions"
  artifacts:
    - path: "src/Audex/UI/ControlBarRenderer.cs"
      provides: "Owner-drawn seek bar, transport buttons, volume slider"
      min_lines: 200
    - path: "src/Audex/UI/LayoutRenderer.cs"
      provides: "Metadata grid with two-column label-value pairs"
      contains: "DrawMetadataGrid"
    - path: "src/Audex/UI/PreviewWindow.cs"
      provides: "AudioPlayer wiring, position timer, mouse event handling for controls"
      contains: "AudioPlayer"
    - path: "src/Audex/PreviewHandler/AudioPreviewHandler.cs"
      provides: "IStream byte copy, AudioPlayer lifecycle, TagReader integration"
      contains: "AudioPlayer"
  key_links:
    - from: "src/Audex/UI/PreviewWindow.cs"
      to: "src/Audex/Audio/AudioPlayer.cs"
      via: "Position timer polls CurrentPositionSeconds, CheckEndOfStream"
      pattern: "_player\\.|AudioPlayer"
    - from: "src/Audex/PreviewHandler/AudioPreviewHandler.cs"
      to: "src/Audex/Audio/AudioPlayer.cs"
      via: "Initialize in constructor, LoadFile in DoPreviewInternal, Shutdown in Unload"
      pattern: "_player\\.Initialize|_player\\.LoadFile|_player\\.Shutdown"
    - from: "src/Audex/PreviewHandler/AudioPreviewHandler.cs"
      to: "src/Audex/Audio/TagReader.cs"
      via: "ReadTags called with byte array + filename in DoPreviewInternal"
      pattern: "TagReader\\.ReadTags"
    - from: "src/Audex/UI/ControlBarRenderer.cs"
      to: "src/Audex/UI/ThemeHelper.cs"
      via: "Theme-aware colors for all control bar elements"
      pattern: "ThemeHelper\\.Get"
    - from: "src/Audex/UI/PreviewWindow.cs"
      to: "src/Audex/UI/ControlBarRenderer.cs"
      via: "OnPaint delegates to ControlBarRenderer.Draw, mouse events to HitTest"
      pattern: "ControlBarRenderer\\."
---

<objective>
Control bar UI (seek bar, transport buttons, volume slider), metadata grid display, and wiring everything together so the user can play audio files from Explorer's preview pane.

Purpose: This plan transforms the Phase 1 placeholder preview into a functional audio player. The user will be able to play/pause/stop audio, seek via the seek bar, control volume, and see metadata from tags and BASS stream info.

Output: ControlBarRenderer.cs (owner-drawn controls), updated LayoutRenderer.cs (metadata grid), updated PreviewWindow.cs (AudioPlayer wiring, timer, mouse events), updated AudioPreviewHandler.cs (byte copy, AudioPlayer lifecycle, TagReader integration), updated ThemeHelper.cs (control bar colors).
</objective>

<execution_context>
@C:/Users/bshap/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/bshap/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-bass-audio-integration/02-RESEARCH.md
@.planning/phases/02-bass-audio-integration/02-CONTEXT.md
@.planning/phases/02-bass-audio-integration/02-01-SUMMARY.md
@src/Audex/Audio/AudioPlayer.cs
@src/Audex/Audio/AudioPlayerState.cs
@src/Audex/Audio/TagReader.cs
@src/Audex/UI/PreviewWindow.cs
@src/Audex/UI/LayoutRenderer.cs
@src/Audex/UI/ThemeHelper.cs
@src/Audex/PreviewHandler/AudioPreviewHandler.cs
@src/Audex/FileReader/AudioFileInfo.cs
@src/Audex/Config/AppConfig.cs
@src/Audex/Config/ConfigManager.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ControlBarRenderer and extend ThemeHelper with control bar colors</name>
  <files>
    src/Audex/UI/ControlBarRenderer.cs
    src/Audex/UI/ThemeHelper.cs
  </files>
  <action>
    1. Add control bar theme colors to `ThemeHelper.cs`:
       - `GetControlBarBackgroundColor()` — same as main background but could differ slightly
       - `GetSeekBarTrackColor()` — Dark: rgb(70,70,70), Light: rgb(200,200,200) — the unfilled track
       - `GetSeekBarFillColor()` — Dark: rgb(0,120,215), Light: rgb(0,100,200) — the filled/elapsed portion (Windows accent blue)
       - `GetSeekBarThumbColor()` — Dark: rgb(255,255,255), Light: rgb(0,100,200) — the thumb circle
       - `GetButtonColor()` — same as text color (icons are drawn with this)
       - `GetButtonHoverColor()` — Dark: rgb(60,60,60), Light: rgb(230,230,230) — background behind hovered button
       - `GetButtonPressColor()` — Dark: rgb(80,80,80), Light: rgb(210,210,210) — pressed state
       - `GetVolumeTrackColor()` — same as seek track color
       - `GetVolumeFillColor()` — Dark: rgb(200,200,200), Light: rgb(100,100,100) — less prominent than seek

    2. Create `src/Audex/UI/ControlBarRenderer.cs`:
       This is a static renderer class (like LayoutRenderer) that draws the bottom control bar.
       It does NOT own state — it receives state and renders it. State is managed by PreviewWindow.

       **Public interface:**
       ```csharp
       public static class ControlBarRenderer
       {
           // Returns the total height of the control bar (DPI-scaled)
           public static int GetControlBarHeight(float dpiScale);

           // Draws the entire control bar
           public static void Draw(Graphics g, Rectangle controlBarBounds,
               AudioPlayerState playerState, double currentPosition, double totalDuration,
               float volume, bool isMuted, HitZone hoveredZone, HitZone pressedZone,
               float dpiScale);

           // Hit-tests a point against control bar elements
           public static HitZone HitTest(Point point, Rectangle controlBarBounds, float dpiScale);
       }

       public enum HitZone
       {
           None,
           SeekBar,
           PlayPauseButton,
           StopButton,
           VolumeIcon,
           VolumeSlider
       }
       ```

       **Layout (per user decisions):**
       - Total height: ~60px at 96 DPI (scaled by dpiScale)
       - Separator line at top (1px, border color)
       - Row 1 (seek bar): Full-width seek bar with time labels flanking
         - Left: elapsed time "1:23" (Segoe UI 8pt, secondary text color)
         - Center: seek bar track (4px height, rounded ends, filled portion + thumb circle)
         - Right: total time "4:56" (Segoe UI 8pt, secondary text color)
         - Time format: use LayoutRenderer.FormatDuration() (already exists)
       - Row 2 (buttons + volume): Transport buttons centered, volume slider right-aligned
         - Transport buttons: Play/Pause toggle + Stop button
         - All buttons same 24x24px (scaled) — per user decision
         - Draw button icons as GDI+ geometric shapes (more reliable than Unicode in prevhost.exe):
           - Play: filled right-pointing triangle
           - Pause: two vertical rectangles
           - Stop: filled square
           - Speaker unmuted: speaker shape (simplified: rectangle + triangle + 2 arcs)
           - Speaker muted: speaker shape + X overlay
         - Hover: draw button background rectangle with GetButtonHoverColor()
         - Press: draw button background with GetButtonPressColor()
         - Volume slider: 80px wide (scaled), 4px track, filled portion + small thumb
         - Volume slider to the right of speaker icon

       **Seek bar interaction areas:**
       Store calculated rectangles for each zone during Draw() in static fields or pass them via a ControlBarLayout struct. HitTest() uses these rectangles to determine which zone was clicked.

       **Key implementation details:**
       - Owner-drawn, theme-aware — per user decision (NOT WinForms TrackBar)
       - Custom-drawn seek bar and volume slider matching style
       - DPI-aware: all pixel values multiplied by dpiScale
       - Use SmoothingMode.AntiAlias for geometric icon shapes
       - Seek bar thumb: 10px diameter circle, visible on hover/drag
       - Volume slider thumb: 8px diameter circle
       - Store last-computed layout rectangles so HitTest works without re-computing

    3. Use `LayoutRenderer.FormatDuration()` for time labels (already exists, public static).
  </action>
  <verify>
    Run `dotnet build src/Audex/Audex.csproj -c Debug` — 0 errors.
    Verify ControlBarRenderer has Draw(), GetControlBarHeight(), HitTest() methods.
    Verify ThemeHelper has all new control bar color methods.
    Verify HitZone enum has SeekBar, PlayPauseButton, StopButton, VolumeIcon, VolumeSlider.
    Verify button icons are drawn as GDI+ shapes (not Unicode text).
  </verify>
  <done>
    ControlBarRenderer draws theme-aware seek bar (with time labels), transport buttons (geometric GDI+ icons), and volume slider (with speaker icon).
    All elements are DPI-scaled.
    HitTest identifies which control zone a point falls within.
    ThemeHelper provides all needed control bar colors.
    Build succeeds with 0 errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire AudioPlayer into PreviewWindow/AudioPreviewHandler, update LayoutRenderer for metadata grid</name>
  <files>
    src/Audex/UI/PreviewWindow.cs
    src/Audex/UI/LayoutRenderer.cs
    src/Audex/PreviewHandler/AudioPreviewHandler.cs
  </files>
  <action>
    1. **Update LayoutRenderer.cs** — Replace placeholder layout with metadata grid + control bar:

       - Remove the "Waveform" and "Controls" placeholder rendering (DrawPlaceholder calls)
       - Change layout proportions: metadata area fills everything above control bar, control bar is fixed height at bottom
       - The metadata area shows:
         a. Filename header (bold, same as current — Path.GetFileNameWithoutExtension)
         b. File size (secondary text, same as current)
         c. Two-column grid/table for metadata (per user decision: grid layout, label-value pairs):
            - "Format" : "WAV" / "MP3" / "FLAC"
            - "Sample Rate" : "44,100 Hz"
            - "Bit Depth" : "16-bit" (skip row if 0)
            - "Channels" : "Stereo" / "Mono" / "{N} channels"
            - "Duration" : "3:45"
            - "Bitrate" : "320 kbps" (skip row if 0)
         d. Tag section (only shown if any tags exist — per user decision: hidden entirely if missing):
            - "Title" : value
            - "Artist" : value
            - "Album" : value
         e. Technical info is most prominent (per user decision) — draw it first, larger/bolder labels

       - Grid rendering: draw labels in secondary text color (left column ~35% width), values in primary text color (right column). Use bodyFont for values, slightly different styling for labels.
       - Per user decision: "Playback coming soon" text should be removed — it's no longer a placeholder.

       - Keep FormatFileSize() and FormatDuration() public static methods (they're reused by ControlBarRenderer).

    2. **Overhaul PreviewWindow.cs** — Add AudioPlayer integration, position timer, mouse events:

       - Add fields:
         - `AudioPlayer? _player` — set externally by AudioPreviewHandler
         - `System.Windows.Forms.Timer _positionTimer` (250ms interval, fires on STA UI thread — per research)
         - `HitZone _hoveredZone = HitZone.None`
         - `HitZone _pressedZone = HitZone.None`
         - `bool _isSeeking = false` (true during seek bar drag)
         - `float _volume` (loaded from config)
         - `bool _isMuted` (loaded from config)

       - Add `SetPlayer(AudioPlayer player)` method — stores reference, subscribes to StateChanged/PlaybackEnded events

       - Update `OnPaint`:
         - Calculate controlBarHeight from `ControlBarRenderer.GetControlBarHeight(dpiScale)`
         - Metadata area: from top to `(bottom - controlBarHeight)`
         - Call `LayoutRenderer.Render()` for metadata area (pass reduced bounds)
         - Call `ControlBarRenderer.Draw()` for control bar (pass control bar bounds, player state, position, duration, volume, muted, hovered/pressed zone)

       - Add `_positionTimer` Tick handler:
         - If player is playing: get position, invalidate control bar area only (not full control)
         - Call `_player.CheckEndOfStream()` to detect track end
         - Start timer on Play, stop on Pause/Stop

       - Add mouse event handlers (OnMouseDown, OnMouseMove, OnMouseUp, OnMouseLeave):
         - Convert point to control bar coordinates
         - Call `ControlBarRenderer.HitTest()` to get zone
         - OnMouseMove: update `_hoveredZone`, change Cursor (Hand for clickable zones)
         - OnMouseDown: set `_pressedZone`, handle:
           - `PlayPauseButton`: toggle play/pause (`_player.Play()` or `_player.Pause()`)
           - `StopButton`: `_player.Stop()`
           - `SeekBar`: calculate ratio from X position, `_player.Seek(ratio * totalDuration)`, set `_isSeeking = true`
           - `VolumeIcon`: toggle mute (`_isMuted = !_isMuted`, apply to player, save config)
           - `VolumeSlider`: calculate volume from X position, `_player.SetVolume()`, save config
         - OnMouseMove with button down:
           - If `_isSeeking`: continuously seek
           - If pressing VolumeSlider: continuously adjust volume
         - OnMouseUp: clear `_pressedZone`, clear `_isSeeking`
         - OnMouseLeave: clear `_hoveredZone`
         - After any state change: `Invalidate()` to trigger repaint

       - PlaybackEnded handler: set state to Stopped visually, stop position timer, invalidate

       - Volume/mute persistence: on volume/mute change, save via ConfigManager.Save()

       - Handle Dispose: stop position timer, unsubscribe from player events

    3. **Update AudioPreviewHandler.cs** — Wire AudioPlayer lifecycle and IStream byte copy:

       - Add fields:
         - `AudioPlayer _player` — created and initialized in constructor
         - `byte[]? _fileData` — the copied IStream bytes

       - In constructor (after PreviewWindow creation):
         - Create `_player = new AudioPlayer()`
         - Call `_player.Initialize()` — if fails, store error state for later display
         - Load volume/mute from ConfigManager: `var config = ConfigManager.Load(); _player.SetVolume(config.IsMuted ? 0f : config.Volume);`
         - Call `_previewWindow.SetPlayer(_player)`

       - Add `CopyStreamToBytes(IStream pstream)` private method:
         - Seek IStream to start: `pstream.Seek(0, 0, out _)` (dwOrigin=0 is STREAM_SEEK_SET)
           Note: IStream.Seek signature is `void Seek(long dlibMove, int dwOrigin, IntPtr plibNewPosition)` — pass IntPtr.Zero for output
         - Read in 64KB chunks into MemoryStream
         - Use StreamHelper pattern from Phase 1, but adapted for bulk reading:
           ```csharp
           var buffer = new byte[65536];
           using var ms = new MemoryStream();
           while (true)
           {
               // Read chunk — IStream.Read takes byte[], count, out int
               pstream.Read(buffer, buffer.Length, out int bytesRead);  // Note: check if this signature matches existing code
               if (bytesRead <= 0) break;
               ms.Write(buffer, 0, bytesRead);
           }
           return ms.ToArray();
           ```
         - IMPORTANT: Check how IStream.Read is called in the existing StreamHelper.cs code — the COM IStream interface may use IntPtr for the bytesRead parameter. Match the existing pattern. If IStream.Read uses `IntPtr plibNewPosition` style, use Marshal.AllocCoTaskMem/Marshal.ReadInt32 pattern from StreamHelper.

       - In `DoPreviewInternal()` (REPLACE current logic):
         - Copy IStream to bytes: `_fileData = CopyStreamToBytes(_stream)`
         - Load file into AudioPlayer: `_player.LoadFile(_fileData, _fileName)` — returns technical metadata
         - Read tags: `var tags = TagReader.ReadTags(_fileData, _fileName)`
         - Build AudioFileInfo from BASS metadata + tags + Phase 1 header parser for bit depth:
           - Use BASS for: sample rate, channels, duration
           - Use Phase 1 header parsers for: bit depth (BASS reports 32-bit float for decode streams — research open question 3)
           - Use TagReader for: title, artist, album
           - Keep format and fileSize from existing IStream stat
         - Update UI via InvokeOnUI with the complete AudioFileInfo
         - If BASS initialization failed: show full error panel (per user decision: "full error panel replacing entire preview content with troubleshooting hint")

       - In `Unload()`:
         - BEFORE releasing IStream: stop audio playback `_player?.Stop()` and free stream `_player?.StopAndFreeStream()` (or add a method that does this without full Shutdown)
         - Actually: call a method that stops playback and frees the current stream but keeps BASS initialized
         - Keep the existing IStream release and state reset
         - Set `_fileData = null` to allow GC

       - Per user decision: when switching files while playing, immediately stop current, load new file ready to play (no crossfade). The LoadFile method in AudioPlayer already handles this (calls StopAndFreeStream first).

       - Keep `_player.Shutdown()` for when the handler is truly being destroyed (could add IDisposable or handle in a destructor, or just call in Unload — but BASS init is expensive, so consider keeping it alive across file switches). Per user decision: "BASS initialized once when handler loads, kept alive until unload." So: DON'T call Shutdown in Unload. Only free the current stream. Call Shutdown in the handler's finalizer or when prevhost.exe unloads the COM object.

       - Revised Unload() approach:
         - `_player.StopAndFreeCurrentStream()` — stops playback, frees stream + GCHandle, sets state to Idle. Does NOT call Bass.Free/BassWasapi.Free.
         - Release IStream, reset state as before
         - BASS stays initialized for next file
  </action>
  <verify>
    Run `dotnet build src/Audex/Audex.csproj -c Debug` — 0 errors.
    Verify LayoutRenderer no longer draws "Waveform" or "Controls" placeholders.
    Verify LayoutRenderer draws metadata in grid/table layout with tag fields.
    Verify PreviewWindow has mouse event handlers for all HitZone types.
    Verify PreviewWindow has position timer (System.Windows.Forms.Timer, 250ms).
    Verify AudioPreviewHandler creates AudioPlayer in constructor and initializes.
    Verify AudioPreviewHandler copies IStream to byte array in DoPreviewInternal.
    Verify AudioPreviewHandler calls TagReader.ReadTags.
    Verify Unload stops audio but does NOT call Bass.Free (BASS stays initialized).
  </verify>
  <done>
    PreviewWindow renders metadata grid above a control bar with seek, transport, and volume controls.
    Mouse events on PreviewWindow trigger play/pause/stop/seek/volume changes via AudioPlayer.
    Position timer polls every 250ms on STA thread and updates seek bar.
    AudioPreviewHandler copies IStream to bytes, feeds to AudioPlayer.LoadFile and TagReader.ReadTags.
    AudioPreviewHandler lifecycle: AudioPlayer.Initialize in constructor, per-file stream in DoPreviewInternal, stream cleanup in Unload (BASS stays alive).
    Volume/mute changes persist to config file.
    Build succeeds with 0 errors.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify audio playback and controls in Explorer</name>
  <files>scripts/register.ps1</files>
  <action>
    Run `scripts/register.ps1` as admin to re-register the updated handler DLL with native BASS DLLs. Then verify all 10 test scenarios listed below in Explorer's preview pane.
  </action>
  <verify>
    Prerequisites: Run `scripts/register.ps1` as admin to re-register the updated handler DLL.

    1. Basic playback (WAV): Select a .wav file in Explorer. Preview should show metadata (sample rate, bit depth, channels, duration, format). Click play button — audio should play through speakers. Click pause — audio pauses. Click play again — resumes from same position. Click stop — resets to start.

    2. MP3 with tags: Select a tagged .mp3 file. Preview should show title, artist, album below technical metadata. Only tags that exist in the file should appear. Play the file — verify audio plays correctly.

    3. FLAC playback: Select a .flac file. Verify it plays and shows correct metadata (FLAC-specific: bit depth should be 16 or 24, not 32).

    4. Seek bar: While playing, click on the seek bar at various positions. Playback should jump to that position. Verify elapsed time updates. Verify total time matches actual file duration.

    5. Volume control: Adjust the volume slider. Audio volume should change independently of Windows system volume. Click speaker icon to mute — audio should go silent. Click again — unmuted at previous level.

    6. Volume persistence: Adjust volume to ~75%. Close Explorer or navigate away from audio files. Return to an audio file — volume should still be at ~75%.

    7. File switching: While playing one file, select a different audio file. Current playback should stop immediately. New file should load ready to play (not auto-playing). Click play to verify new file plays.

    8. Track end behavior: Play a short audio file and let it reach the end. Verify: playback stops, seek bar resets to start, button shows Play (not Pause).

    9. Error handling: If BASS fails to initialize (unlikely but testable by renaming bass.dll temporarily), verify the preview shows a full error panel with a troubleshooting hint instead of crashing.

    10. Theme support: If on dark mode, verify all controls (buttons, seek bar, volume slider) are visible and properly themed. If on light mode, same check.
  </verify>
  <done>
    User confirms audio plays correctly for WAV, MP3, and FLAC files. Transport controls, seek bar, volume slider, and mute toggle all function as expected. Metadata grid shows correct technical info and tags. Volume persists. File switching works cleanly. No crashes.
  </done>
</task>

</tasks>

<verification>
1. `dotnet build src/Audex/Audex.csproj -c Debug` succeeds
2. User can play WAV, MP3, FLAC files from Explorer preview pane
3. Transport controls (play/pause/stop) work correctly
4. Seek bar shows correct elapsed/total time and responds to clicks
5. Volume slider adjusts audio independently of system volume
6. Mute toggle silences/restores audio
7. Volume persists across sessions
8. Switching files stops current playback and loads new file
9. Track end: stops and resets to start
10. Metadata grid shows technical info + tags (tags hidden if missing)
</verification>

<success_criteria>
- User hears audio when clicking play on WAV, MP3, or FLAC file in Explorer preview pane
- Play/Pause toggle, Stop button, seek bar, volume slider, and mute toggle all function correctly
- Metadata grid shows sample rate, bit depth, channels, duration, format, and any available tags
- Volume persists across file selections and Explorer restarts
- No crashes or handle leaks during 10+ file switches
- All controls are theme-aware (dark/light mode)
</success_criteria>

<output>
After completion, create `.planning/phases/02-bass-audio-integration/02-02-SUMMARY.md`
</output>
