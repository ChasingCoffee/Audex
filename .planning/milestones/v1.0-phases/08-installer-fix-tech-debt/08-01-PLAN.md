---
phase: 08-installer-fix-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - installer/Audex.iss
  - src/Audex/PreviewHandler/PreviewHandlerRegistration.cs
  - src/Audex/PreviewHandler/AudioPreviewHandler.cs
  - .planning/phases/05-extended-format-support/05-01-SUMMARY.md
autonomous: true
requirements: [CONF-01, CONF-02, CONF-03, PLAY-04]

must_haves:
  truths:
    - "Installer [Files] section includes Newtonsoft.Json.dll so ConfigManager.Save/Load work on clean install"
    - "ComRegisterFunction writes ThreadingModel=Apartment matching register.ps1 and installer"
    - "Error banner renders when DoPreviewInternal catch block fires (ShowError after UpdateContent)"
    - "05-01-SUMMARY.md accurately describes basswma.dll as real 29KB DLL, not 0-byte placeholder"
  artifacts:
    - path: "installer/Audex.iss"
      provides: "Newtonsoft.Json.dll in [Files] section"
      contains: "Newtonsoft.Json.dll"
    - path: "src/Audex/PreviewHandler/PreviewHandlerRegistration.cs"
      provides: "Correct ThreadingModel value"
      contains: "Apartment"
    - path: "src/Audex/PreviewHandler/AudioPreviewHandler.cs"
      provides: "Correct error banner ordering in catch block"
    - path: ".planning/phases/05-extended-format-support/05-01-SUMMARY.md"
      provides: "Accurate basswma.dll documentation"
  key_links:
    - from: "installer/Audex.iss"
      to: "src/Audex/Config/ConfigManager.cs"
      via: "Newtonsoft.Json.dll runtime dependency"
      pattern: "Newtonsoft\\.Json\\.dll"
    - from: "src/Audex/PreviewHandler/PreviewHandlerRegistration.cs"
      to: "installer/Audex.iss"
      via: "ThreadingModel registry value must match"
      pattern: "Apartment"
    - from: "AudioPreviewHandler.cs catch block"
      to: "PreviewWindow.UpdateContent then ShowError"
      via: "Flag ordering: _showError=false then _showError=true"
---

<objective>
Close four gaps identified in the v1.0 milestone audit: (1) add missing Newtonsoft.Json.dll to installer, (2) align ComRegisterFunction ThreadingModel to "Apartment", (3) fix error banner ordering in catch block, (4) correct basswma.dll documentation.

Purpose: The installer gap is critical -- clean installs currently have no config persistence because Newtonsoft.Json.dll is not packaged. The other three are tech debt items that ensure correctness of COM registration, error rendering, and historical documentation.

Output: Four surgical fixes across four files. No new dependencies, no architectural changes.
</objective>

<execution_context>
@C:/Users/bshap/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/bshap/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-installer-fix-tech-debt/08-RESEARCH.md
@.planning/v1.0-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Newtonsoft.Json.dll to installer and fix ThreadingModel</name>
  <files>installer/Audex.iss, src/Audex/PreviewHandler/PreviewHandlerRegistration.cs</files>
  <action>
**Fix 1 — Installer [Files] section (Audex.iss):**

Add one line after the TagLibSharp.dll entry (currently line 52) and before the "Core BASS native DLLs" comment (currently line 53). The new line is:

```
Source: "..\src\Audex\bin\x64\Release\net48\Newtonsoft.Json.dll"; DestDir: "{app}"; Flags: ignoreversion
```

This keeps managed assembly entries grouped together. Do NOT add it in the optional plugin section or the native DLL section.

**Fix 2 — ThreadingModel (PreviewHandlerRegistration.cs):**

On line 42, change:
```csharp
inprocKey.SetValue("ThreadingModel", "Both");
```
to:
```csharp
inprocKey.SetValue("ThreadingModel", "Apartment");
```

This aligns the ComRegisterFunction with register.ps1 (line 63) and Audex.iss (line 393), both of which already write "Apartment". The change ensures users who run `regasm /codebase` directly (without the installer) get the correct STA-compatible threading model.

Do NOT change any other line in either file.
  </action>
  <verify>
1. Grep installer/Audex.iss for "Newtonsoft.Json.dll" — must find exactly one match in the [Files] section
2. Grep PreviewHandlerRegistration.cs for "Apartment" — must find exactly one match on the ThreadingModel line
3. Grep PreviewHandlerRegistration.cs for "Both" — must find zero matches (the old value is gone)
4. Run `dotnet build src/Audex/Audex.csproj -c Release -p:Platform=x64` — must succeed with 0 errors
  </verify>
  <done>
Audex.iss [Files] section lists 14 managed/native DLLs including Newtonsoft.Json.dll. PreviewHandlerRegistration.cs ComRegisterFunction writes ThreadingModel="Apartment". Build passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix error banner ordering and update basswma.dll documentation</name>
  <files>src/Audex/PreviewHandler/AudioPreviewHandler.cs, .planning/phases/05-extended-format-support/05-01-SUMMARY.md</files>
  <action>
**Fix 3 — Error banner ordering (AudioPreviewHandler.cs):**

In the catch block of DoPreviewInternal (currently lines 433-438), the InvokeOnUI lambda calls ShowError THEN UpdateContent. This is wrong because UpdateContent resets `_showError = false` (PreviewWindow.cs line 197), destroying the error flag that ShowError just set to true (line 228).

Swap the two method calls so UpdateContent runs first, then ShowError. The corrected catch block lambda becomes:

```csharp
InvokeOnUI(() =>
{
    _previewWindow.StopLoading();
    _previewWindow.UpdateContent(_audioFileInfo);
    _previewWindow.ShowError(ex.Message);
});
```

Do NOT modify the success path (lines 365-373) — it already has the correct ordering. Only change the catch block lambda.

**Fix 4 — basswma.dll documentation (05-01-SUMMARY.md):**

Update three locations in the file where basswma.dll is described as a 0-byte placeholder:

1. **key-decisions YAML** (line 49): Change from:
   `"basswma.dll not available at plan URL (404); created 0-byte placeholder per plan fallback policy"`
   to:
   `"basswma.dll initially created as 0-byte placeholder (plan URL 404); subsequently replaced with real 29KB x64 WMA plugin DLL"`

2. **Accomplishments bullet** (line 83): Change the text:
   `basswma.dll is a 0-byte placeholder (download 404)`
   to:
   `basswma.dll: initially a 0-byte placeholder, subsequently replaced with the real 29KB x64 WMA plugin DLL`

3. **Files Created/Modified list** (line 105): Change:
   `src/Audex/native/x64/basswma.dll - WMA BASS plugin (0-byte placeholder, download unavailable)`
   to:
   `src/Audex/native/x64/basswma.dll - WMA BASS plugin (real 29KB x64 DLL; initially 0-byte placeholder)`

4. **Decisions Made bullet** (around line 111): Change reference from:
   `Created 0-byte placeholder per plan's fallback policy. PluginManager detects 0-byte files and skips them with a log message.`
   to:
   `Created 0-byte placeholder per plan's fallback policy; subsequently replaced with the real 29KB DLL. PluginManager loads it normally.`

5. **Issues Encountered** (around line 149): Change reference from:
   `Created 0-byte placeholder per plan fallback policy. WMA format will log "Plugin is a 0-byte placeholder (skipping)" at runtime and return "WMA plugin not found" via GetUnsupportedReason. This can be remedied in a future session by manually placing the correct basswma.dll.`
   to:
   `Created 0-byte placeholder per plan fallback policy. The placeholder was subsequently replaced with the real 29KB basswma.dll, and WMA playback now works normally.`

6. **provides YAML** (line 20): Change:
   `basswma.dll (placeholder)`
   to:
   `basswma.dll (real 29KB x64 WMA plugin)`

7. **tech-stack added** (line 25): Change:
   `basswma.dll (placeholder)`
   to:
   `basswma.dll (x64 WMA plugin)`

Do NOT change any other content in either file. Preserve all other SUMMARY structure, frontmatter, and text exactly as-is.
  </action>
  <verify>
1. Grep AudioPreviewHandler.cs for the pattern "UpdateContent.*ShowError" in the catch block area (lines 430-440) — UpdateContent must appear BEFORE ShowError
2. Grep 05-01-SUMMARY.md for "0-byte placeholder" — must find zero matches (all references updated)
3. Grep 05-01-SUMMARY.md for "29KB" — must find matches confirming updated documentation
4. Run `dotnet build src/Audex/Audex.csproj -c Release -p:Platform=x64` — must succeed with 0 errors
  </verify>
  <done>
AudioPreviewHandler.cs catch block calls UpdateContent before ShowError so the error banner renders correctly. 05-01-SUMMARY.md accurately describes basswma.dll as the real 29KB WMA plugin DLL throughout all references. Build passes.
  </done>
</task>

</tasks>

<verification>
1. `dotnet build src/Audex/Audex.csproj -c Release -p:Platform=x64` passes with 0 errors
2. Newtonsoft.Json.dll appears in Audex.iss [Files] section (grep confirms)
3. "Both" does not appear anywhere in PreviewHandlerRegistration.cs (grep confirms)
4. AudioPreviewHandler.cs catch block has UpdateContent before ShowError (manual inspection of lines ~433-438)
5. No occurrence of "0-byte placeholder" remains in 05-01-SUMMARY.md (grep confirms)
</verification>

<success_criteria>
- Installer [Files] section includes Newtonsoft.Json.dll so config persistence works on clean install
- ComRegisterFunction ThreadingModel matches register.ps1/installer ("Apartment" not "Both")
- Error banner ordering fixed so ShowError renders reliably after UpdateContent resets flag
- basswma.dll documentation in 05-01-SUMMARY.md reflects actual 29KB DLL (not 0-byte placeholder)
- Project builds successfully with 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-installer-fix-tech-debt/08-01-SUMMARY.md`
</output>
